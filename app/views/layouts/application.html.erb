<!DOCTYPE html>
<html>
  <head>
    <title>ClassCollab</title>
    <%# fonts google %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@300..700&display=swap" rel="stylesheet">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= favicon_link_tag "home/logo-footer" %>


    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <%= link_to image_tag('home/logo.png', class: 'logo', alt: 'logo classcollab'), root_path %>
      
      <% if user_signed_in? %>
        <p><%= current_user.email %></p>
        <%= button_to 'Sair', destroy_user_session_path, method: :delete %>
      <% end %>
    </nav>
    <main>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </main>
    <footer>
      <div class="logo-footer">
        <%= image_tag('home/logo-footer.png', class: 'logo', alt: 'logo classcollab') %>
        <p>ClassCollab © 2024 </p>
      </div>
    </footer>
  </body>
</html>
